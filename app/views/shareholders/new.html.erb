<h1>Enter Your Basic Information</h1>

<%= form_for @shareholder, html: { multipart: true } do |f| %>
  <% if @shareholder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shareholder.errors.count, "error") %> prohibited this shareholder from being saved:</h2>

      <ul>
      <% @shareholder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_area :phone %>
  </div>
  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>
